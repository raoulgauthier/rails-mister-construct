
<div class="banner_small" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url('<%= image_path @sinistre.picture%>');">
  <div class="banner-content">

    <h1> <%= @sinistre.name %></h1>

  </div>
</div>

<div class=container>
  <div class=row>
    <div class="panel panel-primary">
      <div class="panel-heading">
         <h3> Informations importantes</h3>
      </div>
      <div class="panel-body">
          <ul>
            <% @sinistre.repartitions.each do |repartition| %>
              <li>
                <h3>Qui contacter en premier ?</h3>

                <p> Le <%= repartition.metier.name %> s'occupe à <%= repartition.description %> de la résolution de ce type de sinistre. Il est impératif de contacter un <%= repartition.metier.name %> le plus rapidement possible </p>
                <%= link_to repartition_path(repartition), method: :delete do %>
                <i class="fa fa-trash" aria-hidden="true"></i>
                <% end %>
              </li>
            <% end %>


              <li>
                <h3>Quelles démarches dois-je suivre ?</h3>
                <%= @sinistre.demarches %>
              </li>

              <li>
                <h3>Comment suis-je couvert ?</h3>
                <%= @sinistre.couverture%>
              </li>
          </ul>
      </div>
    </div>
    <%= link_to "Ajouter un intervenant et son importance", new_sinistre_repartition_path(@sinistre) %>
  </div>
</div>
